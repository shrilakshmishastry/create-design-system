import React, { FC } from "react";
// eslint-disable-next-line no-unused-vars
import { Link } from "react-router-dom";
const ThemeNavigator: FC = () => {
  return (
    <>
      <p>Theme</p>

      {/* theme */}
    </>
  );
};
export default ThemeNavigator;
